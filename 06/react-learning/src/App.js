// 导入react 并且 解构 Component
import React,{Component} from "react";
export default class App extends Component {
  constructor(props) {
    super(props)
  }
  render () {
    // render 返回值
    // 1.可以返回 react 元素 dom 节点 react component
    // 2. array 数组
    // 3.string 字符串
    // 4.布尔值和null 什么都不显示
    return null
  }
}
